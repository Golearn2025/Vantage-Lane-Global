"use client";

import { PartnerCoverageForm } from "@/modules/partner/components/coverage-form";

/** Wizard step — persists to offering_coverages (same as Add Operator). */
export function AvailabilityStep() {
  return <PartnerCoverageForm />;
}
